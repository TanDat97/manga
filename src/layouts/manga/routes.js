// core component
import Dashboard from "views/manga/Dashboard";

const rootRoutes = [
  {
    name: "Dashboard",
    component: Dashboard,
    layout: "",
    path: "/",
    root: true,
  },
];

export default rootRoutes;
